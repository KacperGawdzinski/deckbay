import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#35383d",
      light: "#5f6267",
      dark: "#0f1217",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
